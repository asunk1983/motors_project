Создан исчерпывающий технический снимок проекта в **docs/PROJECT_SNAPSHOT.md** (74,831 байт).

**Что сделано:**
1. **Проанализировано ~65 исходных файлов** проекта (каждый прочитан целиком один раз)
2. **Выполнены Select-String поиски** по всему репозиторию для карты использования констант/функций (разделы 6, 7, 15)
3. **Заполнены все 18 разделов** документа строго по порядку:
   - 0. Метаданные (git status, Python 3.11.9, pip freeze)
   - 1. Полное дерево проекта
   - 2. Архитектура + 10 зарегистрированных blueprint'ов
   - 3. Полная схема БД (7 таблиц с полями, FK, индексами, ON DELETE CASCADE поведением)
   - 4. Карта всех 50+ роутов по 11 файлам routes/
   - 5. Подписи всех публичных функций repositories/services/modules + содержимое ВСЕХ init.py
   - 6. Константы config.settings.py + таблица импортов/дублей (10 уникальных путей, ~45 использований)
   - 7. Контракт имён фото: актуальная схема `ID{engine_id}_{n}.{ext}` + найдено дублирование в export_service (старая схема `engine_{engine_id}_`)
   - 8. Жизненный цикл ID: _next_free_id (переиспользование дыр) vs массовый импорт (executemany на пустую БД)
   - 9. /api/clear: явное удаление детей, сброс sqlite_sequence, WAL checkpoint + VACUUM, shutil.rmtree
   - 10-13. Frontend: 14 JS-файлов, 13 глобальных переменных, 35+ ключевых функций, 15 UI-компонентов
   - 14. Тесты: структура, команды запуска, ~65 сценариев
   - 15. Карта использования (Select-String результаты для photo_manager, PHOTOS_FOLDER, DB_PATH, ALLOWED_PHOTO_EXT, FILE_USER_ID_OFFSET, MAX_WORKERS, ENGINE_COLUMNS)
   - 16. **10 найденных расхождений** (две схемы фото, локальный DB_PATH, неиспользуемая normalize_base_name, неработающий CASCADE, отсутствие проверки пустой БД при импорте, дублирование путей фото, несинхронизированные init.py, нарушение паттерна динамического чтения, смешанные подходы в reset_admin_password, тестовые дубли в temp/)
   - 17. 7 известных особенностей/инцидентов
   - 18. Итоговая сводка метрик