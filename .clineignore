# === Системные и служебные директории ===
.git/
.venv/
venv/
env/
node_modules/
dist/
build/

# === Кэш Python и тестовые кэши ===
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# === Папки с исходными данными (не код) ===
# Загруженные фотографии, Excel-файлы, резервные копии, staging
motors/
photos/
backups/
backupstaging/
backup_staging/

# === Файлы баз данных ===
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# === Сгенерированные архивы ===
*.zip

# === Логи и файлы логов ===
*.log
import_log.txt

# === Переменные окружения и файлы с учётными данными ===
.env
config/tokens.json
config/users.json

# === Временные файлы и директории ===
temp/
temp_*/

# === Одноразовые/случайные файлы в корне ===
itogo.bat
*.cmd
