### �� 📊 Общая оценка проекта (Резюме)
Краткая оценка состояния кодовой базы от 1 до 10 по категориям: Чистота, Скорость, Архитектура.

- **Чистота**: 6 - Есть дублирующийся код (например, `_formatRuDate`), мертвые файлы (`1auth.js`, `app.js`), несогласованные импорты путей.
- **Скорость**: 7 - В целом хорошая производительность, но есть потенциальные узкие места: сканирование файловой системы для фото, дублирующиеся обработчики событий, отсутствие кэширования в некоторых местах.
- **Архитектура**: 8 - Архитектура в целомsound, с четким разделением обязанностей, но есть нарушения контрактов (жестко прописанные пути) и использование AUTOINCREMENT в импорте (хотя и с оговоркой).

### �� 🚨 [CRITICAL] Критические проблемы и баги
(Проблемы, способные вызвать падение сервера, потерю данных или утечки)
- **Файл:** `/mnt/c/motors_project/diag_photos.py:7`
  **Проблема:** Жестко прописанный путь к базе данных `'engine_data.db'` вместо использования `config.settings.DB_PATH`.
  **Предлагаемое решение:** Заменить строку `conn = sqlite3.connect('engine_data.db')` на `from config.settings import DB_PATH; conn = sqlite3.connect(DB_PATH)`.

- **Файл:** `/mnt/c/motors_project/promote_and_cleanup.py:10` (примерная строка)
  **Проблема:** Определение `DB_PATH = Path(__file__).parent / 'engine_data.db'` вместо импорта из `config.settings`.
  **Предлагаемое решение:** Удалить локальное определение и использовать `from config.settings import DB_PATH`.

### �� ⚠��️ [WARNING] Архитектурные риски и кодовые запахи (Code Smells)
(Дублирование, нарушение контрактов, потенциальные проблемы производительности)
- **Файл:** `/mnt/c/motors_project/static/js/1auth.js` - Дублирование функционала авторизации (почти идентичен `auth.js`), но не используется нигде в проекте. Мертвый код.
- **Файл:** `/mnt/c/motors_project/static/js/app.js` - Не используется нигде в проекте (никаких ссылок в HTML или других JS-файлах). Мертвый код.
- **Файл:** `/mnt/c/motors_project/static/js/backupManager.js:225-234` - Дублирование функции `_formatRuDate`, которая уже определена в `common.js` и импортируется (как указано в комментарии строки 2). Локальное определение тенит глобальную функцию и приводит к избыточному коду.
- **Файл:** `/mnt/c/motors_project/repositories/engine_repo.py:25-33` - Функция `get_with_details` выполняет два отдельных запроса для получения режимов и работ. Хотя используется только для одного двигателя (по ID), при необходимости вывода списка двигателей с деталями приведет к проблеме N+1.
- **Файл:** `/mnt/c/motors_project/routes/import_routes.py:117-130` - Массовый импорт использует `AUTOINCREMENT` (не указывает `id` при `INSERT`), что нарушает правило генерации ID через `_next_free_id()`. Однако импорт защищен проверкой на пустую БД, поэтому риск низкий, но нарушает архитектурное соглашение.

### �� ⚡ [OPTIMIZATION] Точки роста производительности
(Оптимизация SQL, DOM, кеширование, минимизация I/O)
- **Файл:** `/mnt/c/motors_project/static/js/engineCard.js` - В функциях crop-поддержки добавлены обработчики `document.addEventListener('pointermove', ...)` и `document.addEventListener('pointerup', ...)` без явного удаления при закрытии модалки (очистка происходит через `window.closeTimeout`, но не для этих слушателей). Это может привести к утечке памяти или непреднамеренному срабатыванию при повторных открытиях модалки.
- **Файл:** `/mnt/c/motors_project/static/js/catalog.js` - Обработчик изменения размера окна (`window.addEventListener('resize', debouncedApplyDynamicPageSize)`) добавлен при инициализации, но никогда не удаляется. В SPA это не критично, так как страница не перезагружается, но при частом изменении размера окна может prowadжать к лишним вызовам.
- **Файл:** `/mnt/c/motors_project/modules/photo_manager/manager.py` - Функция `engine_photo_disk_paths` выполняет сканирование файловой системы glob для каждого вызова. При большом количестве фото и частых вызовах (например, при рендере списка двигателей) это может стать узким местом. Рассмотреть кэширование результатов на уровне двигателя с инвалидацией при изменении фото.
- **Файл:** `/mnt/c/motors_project/modules/db.py` - В функции `init_db` создаются отдельные индексы для каждой текстовых колонки двигателя. Рассмотреть составные индексы для частых комбинаций фильтров (например, `location` + `engine_type`) на основе анализа типичных запросов.

### �� 🧹 [CLEANUP] Мертвый код и гигиена
(Файлы и функции на удаление/рефакторинг)
- Удалить файл `/mnt/c/motors_project/static/js/1auth.js` (дубликат `auth.js`, не используется).
- Удалить файл `/mnt/c/motors_project/static/js/app.js` (не используется нигде).
- В файле `/mnt/c/motors_project/static/js/backupManager.js` удалить дублирующую функцию `_formatRuDate` (строки 225-234) и использовать версию из `common.js` (удалить локальное определение, оставить вызов глобальной функции).
- Рассмотреть удаление или рефакторинг файлов `/mnt/c/motors_project/diag_photos.py` и `/mnt/c/motors_project/promote_and_cleanup.py` если они не используются в основной работе приложения (в настоящее время они не импортируются ни в одном месте кода, за исключением упоминаний в документации). Если они являются скриптами для администратора, перенести их в папку `scripts/` и обеспечить использование `config.settings` для путей.

### План действий (3–5 самых важных шагов)
1. **Устранить критические нарушения путей**: Исправить `diag_photos.py` и `promote_and_cleanup.py` на использование `config.settings.DB_PATH` (или удалить, если не используются).
2. **Удалить мертвый код и дубликаты**: Удалить `1auth.js`, `app.js` и дублирующее `_formatRuDate` в `backupManager.js`.
3. **Оптимизировать рендеринг фото**: Ввести кэширование результатов `engine_photo_disk_paths` с инвалидацией при загрузке/удалении фото, чтобы уменьшить нагрузку на файловую систему.
4. **Исправить потенциальные утечки слушателей**: В `engineCard.js` обеспечить удаление обработчиков `pointermove`, `pointerup` и `click` при закрытии модалки кадрирования и деталей.
5. **Улучшить архитектуру импорта**: Рассмотреть возможность использования `_next_free_id` даже в массовом импорте для единообразия, оставив проверку на пустую БД как предосторожность.

Все исправления должны быть выполнены с соблюдением правил из `CLAUDE.md`: использование только из `config.settings` для путей, запрет на относительные строки, сохранение схемы именования фото и т.д.